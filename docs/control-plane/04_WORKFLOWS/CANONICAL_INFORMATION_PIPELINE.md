# Canonical Information Pipeline

INPUT → NORMALIZE → NOTEBOOK → DERIVATIVES → PRESENTATION

Any change upstream must trigger regeneration downstream.

No manual edits to generated artifacts.
No silent overrides.
